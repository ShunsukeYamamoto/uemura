%h1 お知らせ一覧
- if Notice.exists?
  - @notices.each do |n|
    %table
    %tr
      %td #{n.title}
      %td #{n.created_at}
      %td 
        =link_to "編集",edit_notice_path(n)
      %td
        =link_to "削除",notice_path(n),method: :delete
- else 
  %p 投稿はありません

=link_to "戻る",orders_path